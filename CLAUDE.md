# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(claude.ai/code)에게 제공하는 가이드입니다.

## 프로젝트

"믿먹" — 맛집 리뷰 서비스 컨셉("믿고 먹는 맛집" / 광고·가짜 리뷰 없는 신뢰할 수 있는 맛집 발견 서비스)의 랜딩페이지입니다. 현재 이 저장소에는 랜딩페이지만 있고, 실제 서비스 본체는 포함되어 있지 않습니다.

`PRD.md`와 `DESIGN.md`가 콘텐츠와 비주얼 스펙의 기준 문서입니다. 구조나 콘텐츠를 변경하기 전에 먼저 읽으세요.
- `PRD.md`는 서비스 컨셉, 랜딩페이지에 필요한 섹션 구성(헤더 → 히어로 → 데모 체험 → 로드맵 티저 → CTA), 그리고 랜딩 이후의 MVP/로드맵 범위(이 저장소에는 아직 없음)를 정의합니다.
- `DESIGN.md`는 무드(에디토리얼/매거진), 컬러(흑백 베이스 + `#FFF67E` 포인트 컬러, 기능적 요소에만 제한적으로 사용), 타이포그래피(세리프 제목 / 산세리프 본문), 카드 캐러셀 및 리뷰 상세 인터랙션 레퍼런스를 정의합니다.

## 명령어

빌드 단계, 패키지 매니저, 테스트 스위트가 없습니다 — 의존성 없는 정적 HTML/CSS/JS 사이트입니다.

- **로컬 실행**: `index.html`을 브라우저에서 바로 열면 됩니다 (`file://`로 동작, 서버 불필요).
- 실행할 lint/build/test 명령어는 없습니다.

## 아키텍처

프레임워크 없는 순수 멀티파일 정적 사이트입니다.

- `index.html` — 모든 섹션(헤더, 히어로, 데모, 로드맵, CTA, 푸터)과 리뷰 상세 모달 마크업이 한 파일에 들어 있습니다.
- `css/style.css` — 모든 스타일. 폰트/컬러/컨테이너 너비 같은 디자인 토큰은 파일 상단 `:root`에 CSS 커스텀 프로퍼티로 한 번만 정의되고 전체에서 재사용됩니다. 팔레트나 타이포그래피를 바꿀 때는 개별 규칙에 값을 하드코딩하지 말고 이 토큰을 수정하세요. 모바일 퍼스트이며, 태블릿/데스크톱 레이아웃 전환을 위한 브레이크포인트는 `768px` 하나입니다.
- `js/demo-data.js` — 정적 목업 데이터만 담당: 필터 칩을 만드는 데 쓰이는 `RESTAURANTS` 배열(id, name, category, area, tag, 이미지 경로, 리뷰 스니펫)과 `CATEGORIES` 목록. `main.js`보다 먼저 로드되어야 합니다(순서 중요 — `main.js`는 이 값들을 전역 변수로 읽으며, import/번들러가 없습니다).
- `js/main.js` — 데모 섹션의 모든 인터랙션 로직을 IIFE로 감싸 구현합니다: `CATEGORIES`로 필터 칩을 렌더링하고, 활성 카테고리 + 디바운스된 검색어(AND 조건)로 `RESTAURANTS`를 필터링해 카드를 렌더링하며, scroll-snap 카드 캐러셀의 이전/다음 화살표를 제어하고, 리뷰 상세 모달을 열고 닫습니다(카드 클릭 → 모달 오픈, ×/오버레이 클릭/Escape로 닫기).
- `assets/images/` — 배포된 사이트가 외부 이미지 런타임 의존성을 갖지 않도록 모든 이미지를 다운로드해 저장소에 커밋했습니다(핫링크 아님). 히어로 섹션용 `hero.jpg`, 데모 카드용 `restaurants/01.jpg`~`09.jpg`가 있으며 `demo-data.js`에서 경로로 참조됩니다.

### 데모 섹션 데이터 흐름

`demo-data.js`(`RESTAURANTS`, `CATEGORIES`) → 로드 시 `main.js`가 칩/카드를 렌더링 → 사용자 인터랙션(칩 클릭 / 검색 입력) → `main.js`가 메모리상의 배열을 다시 필터링해 카드 리스트를 재렌더링 — 네트워크 호출이나 백엔드는 없습니다. 목업 맛집을 추가하려면 `demo-data.js`의 `RESTAURANTS`에 항목을 추가하고(그리고 `assets/images/restaurants/`에 이미지 파일을 넣고) 그 외 파일은 새 카테고리를 도입하는 경우가 아니라면 수정할 필요가 없습니다(새 카테고리라면 `CATEGORIES`에도 추가).

## 반응형
- 모바일 (375)
- 태블릿 (768)
- 데스크탑 (1440)
으로 브레이크 포인트 설정